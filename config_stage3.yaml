# ============ 模型 ============
model: runs/train/stage2/weights/best.pt  # 加载 stage2 最优权重继续训练

# ============ 数据集 ============
path: data/radar_dataset
train: images/train
val: images/val
names:
  0: hero-red
  1: engineer-red
  2: infantry3-red
  3: infantry4-red
  4: sentry-red
  5: drone-red
  6: hero-blue
  7: engineer-blue
  8: infantry3-blue
  9: infantry4-blue
  10: sentry-blue
  11: drone-blue

# ============ 训练超参 ============
epochs: 700                    # 第三轮训练 700 epoch
batch: 128
imgsz: 1280                    # 回到高分辨率精调
device: 0,1,2,3,4,5,6,7
workers: 32
lr0: 0.0005                    # 进一步降低学习率
lrf: 0.0005
momentum: 0.937
weight_decay: 0.0005
warmup_epochs: 3
warmup_momentum: 0.8
warmup_bias_lr: 0.1
box: 7.5
cls: 0.5
dfl: 1.5
patience: 50
save: true
save_period: 10
val: true
amp: true
cos_lr: false
close_mosaic: 10

# ============ 日志与可视化 ============
project: runs/train
name: stage3
exist_ok: true
plots: true
resume: false

